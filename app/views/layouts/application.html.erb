<!DOCTYPE html>
<html>
  <head>
    <title>Garden</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% unless flash.empty? %>
      <div class="container mx-auto">
        <% if notice.present? %>
          <p class="py-2 px-3 bg-green-50 m-2 mb-5 text-green-700 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
        <% end %>
        <% if alert.present? %>
          <p class="py-2 px-3 bg-red-50 m-2 mb-5 text-red-700 font-medium rounded-lg inline-block" id="alert"><%= alert %></p>
        <% end %>
      </div>
    <% end %>

    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
